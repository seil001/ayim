import React from "react";
import Navbar from "../components/Navbar";

const MainLayout = () => {
  // const user = getUser();
  return (
    <div>
      <Navbar />
    </div>
  );
};

export default MainLayout;
